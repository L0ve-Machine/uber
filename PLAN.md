# 実装計画: チェックアウト画面 & 注文追跡機能

## 概要

### 実装対象
1. **5-1: チェックアウト画面の完成** - 決済フロー
2. **5-3: 注文追跡機能** - リアルタイムステータス更新

### 現状分析

**既に実装済み:**
- バックエンドAPI: 注文作成、注文一覧、注文詳細、キャンセル
- バックエンドAPI: 住所管理（CRUD）
- Flutter: カート画面、カートプロバイダー
- Flutter: 注文履歴画面、注文詳細画面
- Flutter: OrderModel, AddressModel
- Flutter: OrderApiService, OrderRepository
- Flutter: ルート定義（checkout, order-tracking）

**未実装:**
- Flutter: チェックアウト画面UI
- Flutter: 住所選択・管理UI
- Flutter: 住所プロバイダー
- Flutter: 注文追跡画面（ステータスタイムライン）
- Flutter: 注文確認画面

---

## Phase 1: チェックアウト画面 (5-1)

### 1.1 住所管理機能

#### 新規ファイル作成

**`lib/features/customer/data/services/address_api_service.dart`**
```dart
- getAddresses(customerId)
- addAddress(...)
- updateAddress(...)
- deleteAddress(id)
- setDefaultAddress(id)
```

**`lib/features/customer/data/repositories/address_repository.dart`**
```dart
- AddressRepository wrapping AddressApiService
```

**`lib/features/customer/providers/address_provider.dart`**
```dart
- addressListProvider: 住所一覧取得
- selectedAddressProvider: 選択中の住所
- addAddressAction
- setDefaultAddressAction
```

### 1.2 チェックアウト画面

**`lib/features/customer/screens/checkout_screen.dart`**

UI構成:
```
┌─────────────────────────────────┐
│ ← チェックアウト                 │
├─────────────────────────────────┤
│ 配達先住所                       │
│ ┌─────────────────────────────┐ │
│ │ 🏠 自宅                      │ │
│ │ 東京都渋谷区...              │ │
│ │                    [変更 >] │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 注文内容                        │
│ ┌─────────────────────────────┐ │
│ │ 🍕 マルゲリータ x2    ¥2,400│ │
│ │ 🍝 ペペロンチーノ x1   ¥1,200│ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 特別リクエスト                   │
│ [                            ]  │
├─────────────────────────────────┤
│ お支払い方法                     │
│ ○ クレジットカード              │
│ ● 代金引換                      │
├─────────────────────────────────┤
│ 料金詳細                        │
│ 小計                    ¥3,600  │
│ 配送料                    ¥300  │
│ 消費税                    ¥360  │
│ ─────────────────────────────  │
│ 合計                    ¥4,260  │
├─────────────────────────────────┤
│ [      注文を確定する     ]     │
└─────────────────────────────────┘
```

### 1.3 住所選択画面

**`lib/features/customer/screens/address_selection_screen.dart`**

UI構成:
```
┌─────────────────────────────────┐
│ ← 配達先を選択                  │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ ✓ 🏠 自宅 (デフォルト)      │ │
│ │   東京都渋谷区神南1-2-3     │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │   🏢 会社                   │ │
│ │   東京都港区六本木4-5-6     │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ [+ 新しい住所を追加]            │
└─────────────────────────────────┘
```

### 1.4 住所追加画面

**`lib/features/customer/screens/add_address_screen.dart`**

フォームフィールド:
- ラベル（自宅/会社/その他）
- 住所（address_line）
- 市区町村（city）
- 郵便番号（postal_code）
- デフォルトに設定（チェックボックス）

### 1.5 注文確認画面

**`lib/features/customer/screens/order_confirmation_screen.dart`**

```
┌─────────────────────────────────┐
│         ✓ 注文完了！            │
│                                 │
│     注文番号: ORD-20251127-0001 │
│                                 │
│     ご注文ありがとうございます   │
│     準備ができ次第お届けします   │
│                                 │
│ [   注文を追跡する   ]          │
│ [   ホームに戻る     ]          │
└─────────────────────────────────┘
```

---

## Phase 2: 注文追跡機能 (5-3)

### 2.1 注文追跡画面

**`lib/features/customer/screens/order_tracking_screen.dart`**

UI構成:
```
┌─────────────────────────────────┐
│ ← 注文追跡                      │
├─────────────────────────────────┤
│ 注文番号: ORD-20251127-0001     │
│ レストラン: イタリアンビストロ    │
├─────────────────────────────────┤
│ ステータス                      │
│                                 │
│ ✓ 注文受付      14:30          │
│ │                              │
│ ✓ 調理中        14:35          │
│ │                              │
│ ○ 配達準備完了                  │
│ │                              │
│ ○ 配達中                       │
│ │                              │
│ ○ 配達完了                     │
│                                 │
├─────────────────────────────────┤
│ 配達員情報（配達中の場合）       │
│ ┌─────────────────────────────┐ │
│ │ 👤 田中太郎                  │ │
│ │ ⭐ 4.8                       │ │
│ │ [📞 電話] [💬 メッセージ]    │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 配達先                          │
│ 東京都渋谷区神南1-2-3           │
├─────────────────────────────────┤
│ 注文内容                        │
│ マルゲリータ x2         ¥2,400  │
│ ペペロンチーノ x1       ¥1,200  │
│ ─────────────────────────────  │
│ 合計                   ¥4,260   │
├─────────────────────────────────┤
│ [  注文をキャンセル  ]          │
└─────────────────────────────────┘
```

### 2.2 ステータスタイムラインウィジェット

**`lib/features/customer/widgets/order_status_timeline.dart`**

ステータス一覧:
1. `pending` - 注文受付
2. `accepted` - 店舗確認済み
3. `preparing` - 調理中
4. `ready` - 配達準備完了
5. `picked_up` - 配達中
6. `delivered` - 配達完了
7. `cancelled` - キャンセル

### 2.3 注文追跡プロバイダー更新

**`lib/features/customer/providers/order_tracking_provider.dart`**

```dart
- activeOrderProvider: 進行中の注文を取得
- orderTrackingProvider(orderId): 特定の注文のリアルタイム追跡
  - 定期的なポーリング（30秒ごと）でステータス更新
```

---

## 実装ファイル一覧

### 新規作成（Flutter）

| ファイル | 説明 |
|---------|------|
| `lib/features/customer/data/services/address_api_service.dart` | 住所API |
| `lib/features/customer/data/repositories/address_repository.dart` | 住所リポジトリ |
| `lib/features/customer/providers/address_provider.dart` | 住所プロバイダー |
| `lib/features/customer/screens/checkout_screen.dart` | チェックアウト画面 |
| `lib/features/customer/screens/address_selection_screen.dart` | 住所選択画面 |
| `lib/features/customer/screens/add_address_screen.dart` | 住所追加画面 |
| `lib/features/customer/screens/order_confirmation_screen.dart` | 注文確認画面 |
| `lib/features/customer/screens/order_tracking_screen.dart` | 注文追跡画面 |
| `lib/features/customer/widgets/order_status_timeline.dart` | ステータスタイムライン |

### 修正（Flutter）

| ファイル | 修正内容 |
|---------|---------|
| `lib/features/customer/screens/cart_screen.dart` | チェックアウトへの遷移 |
| `lib/features/customer/providers/order_provider.dart` | 追跡用プロバイダー追加 |
| `lib/core/routes/app_router.dart` | 新規ルート追加 |
| `lib/features/auth/providers/auth_provider.dart` | customerId取得用 |

---

## 実装順序

1. **住所API・リポジトリ・プロバイダー** - 基盤
2. **住所選択・追加画面** - UI
3. **チェックアウト画面** - メイン機能
4. **注文確認画面** - 注文後の確認
5. **注文追跡画面** - ステータス表示
6. **ルート設定・遷移修正** - 全体統合
7. **テスト・動作確認**

---

## 注意事項

- 決済は現時点では「代金引換（cash）」のみ対応
- Stripe統合は別フェーズで実装予定
- リアルタイム更新はポーリング方式（WebSocket/Firebaseは別フェーズ）
- 地図表示は別フェーズで実装予定
